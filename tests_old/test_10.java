public class OperatorExample {
    public boolean add( int a, int b){
        return true;
    }
    public static void main() {
        // arithmetic operators
        int a = 10;
        byte b;
        short v = 87, g=90, y;
        // b = 9;
        int c = a + b;
        int d = a - b;
        int result = (a > b) ? c*c+d : d*a+b-c, t=90;
        // int y, g=10;
        // int j, k, hh=10, m;
        // String result2 = (a*a < b*b ) ? add(3,5) : a;
        // d = a;
        // p = d+a*b;
        // int g = add(2,4);
        // a = b;
        // int f, g=9,h=9;
        // int a = 10;
        // int b = 20;
        
        // byte b = 5+5;
        // int lkj = 7.9;
        // int e = a * b;
        // int f = a / b;
        // int g = a % b;
        // int ab = ++a;
        // int acc = a++;
        // int accc = --a;
        // int acccc = a--;
        // int ac = !++a;
        // int ad = x+y;
        // int axcz = +a--;
        // int axd = +a--;
        // int abh = +--a;
        // boolean ag = true;
        // int k = ++ag;

        // // increment and decrement operators
        // int h = ++a;
        // int i = --b;
        // int j = c++;
        // boolean k = d--;
        
        // // relational operators
        // boolean l = (a == b);
        // boolean m = (a != b);
        // boolean n = !(a > ++b);
        // boolean o = (g == k);
        // boolean p = (g != ++a);
        // boolean q = (6.0 != 6);
        // boolean o = (a < n++);
        // boolean p = (a >= b);
        // boolean q = (a <= b);
        
        // // bitwise operators and shifting operators
        // int r = (a & b);
        // int s = (a | b);
        // int t = (a ^ b);
        // int u = (~a);
        // int v = (a << 2);
        // byte w = (a >> 2);
        // int x = (a >>> 2.0);
        
        // // logical operators
        boolean yyy = (a > b) && (c < d);
        boolean zzz = (a < b) || (c > d);
        boolean aaaa = !(a > b);
        boolean ttt = !aa;

        // // assignment operators
        a += b;
        b -= c;
        c *= d;
        d /= e;
        e %= f;
        f &= g;
        aa *= 2;
        
        // // ternary operator
        
        // int af = +a;
        // int ae = +(a+4-8);

        int f = ((a+1)*(a+1))+~(6*9-7+9);
        int f = 2+-1*3/-8+9*78;

        byte ee = 4;
        int xx = ~ee;
        int vv = ~1;
        char aaa = 'a';
        int bb = ~a;
        int ff = ~1.9;
        int fgh = 3.0/-8;
        int gg = 3.0/-8.0;
        int hh = 3/-8;
        int gg = 3.4%5.6;
        int ii = 3/-8.0;
    }
}
